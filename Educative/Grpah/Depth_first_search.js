const Graph = require('./graph');
const LinkedList = require('../LinkedList/core/LinkedList');
const Queue = require('../Stacks/queue');

const printer = [];



const dfs_traversal = (graph) => {

}











const g = new Graph();
g.addEdge(0, 1);
g.addEdge(0, 2);
g.addEdge(1, 3);
g.addEdge(1, 4);
g.addEdge(2, 5);

dfs_traversal(g);